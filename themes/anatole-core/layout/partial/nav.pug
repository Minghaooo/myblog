.page-top.animated.fadeInDown
  .nav
    li
      if is_home()
        a.current(href="/")= __('Home')					
      else
        a(href="/")= __('Home')	

    li
      if is_archive()
        a.current(href="/myblog/archives")= __('Archives')
      else
        a(href="/myblog/archives")= __('Archives')

    li
      if is_current('tags')
        a.current(href="/myblog/tags")= __('Tags')     
      else
        a(href="/myblog/tags")= __('Tags')

    li
      if is_current('about')
        a.current(href="/myblog/about")= __('About')     
      else
        a(href="/myblog/about")= __('About')

    li
      if is_current('guestbook')
        a.current(href="/myblog/guestbook")= __('Guestbook')					
      else
        a(href="/myblog/guestbook")= __('Guestbook')		


  .information
    .back_btn
      li
        if is_post()
          a.fa.fa-chevron-left(onclick="window.history.go(-1)") 
        else
          a.fa.fa-chevron-left(onclick="window.history.go(-1)",style="display:none;") 
